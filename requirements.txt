# To generate a new / updated requirements.txt:
# Make sure you are in your development environment and the project directory
# Delete the existing requirements.txt file to be replaced
# Install pipreqs by running $ pip install pipreqs
# Once installed, run $ pipreqs ~/path/to/project

Flask==0.12.2
flask_sqlalchemy==2.3.1
google_auth==1.1.1
pymysql==0.7.11
protobuf==3.4.0
requests==2.18.4
pyzipcode3==2.0
gunicorn==19.7.1
pytest-flask==0.10.0
